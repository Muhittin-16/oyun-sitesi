// OynaKazan - Ana JavaScript document.addEventListener("DOMContentLoaded", function () { let score = Number(localStorage.getItem("oynakazan_score")) || 0; const scoreElement = document.getElementById("user-score"); const watchAdButton = document.getElementById("watch-ad-btn"); const modal = document.getElementById("game-modal"); const iframe = document.getElementById("game-iframe"); const modalTitle = document.getElementById("modal-game-title"); function updateScore() { if (scoreElement) { scoreElement.textContent = score; } localStorage.setItem("oynakazan_score", score); } updateScore(); // Reklam butonu if (watchAdButton) { watchAdButton.addEventListener("click", function () { alert( "Reklam sistemi henüz bağlanmadı.\n\n" + "Reklam ağı onaylandıktan sonra bu bölüm gerçek reklamlarla çalışacak." ); }); } // Kategori filtreleri const categoryButtons = document.querySelectorAll(".cat-btn"); const gameCards = document.querySelectorAll(".game-card"); categoryButtons.forEach(function (button) { button.addEventListener("click", function () { categoryButtons.forEach(function (btn) { btn.classList.remove("active"); }); button.classList.add("active"); const filter = button.dataset.filter; gameCards.forEach(function (card) { if (filter === "all" || card.dataset.category === filter) { card.style.display = ""; } else { card.style.display = "none"; } }); }); }); // Oyun açma window.openGame = function (gameName, gameUrl) { if (!modal || !iframe || !modalTitle) { return; } modalTitle.textContent = gameName; // Gerçek oyun adresi henüz eklenmemişse bilgi göster if (!gameUrl || gameUrl.includes("...")) { iframe.srcdoc = ` <div style=" min-height:100%; display:flex; align-items:center; justify-content:center; background:#0f172a; color:white; font-family:Arial,sans-serif; text-align:center; padding:30px; "> <div> <h2 style="margin-bottom:15px;">🎮 ${gameName}</h2> <p>Bu oyunun gerçek bağlantısı henüz eklenmedi.</p> <p style="margin-top:10px;color:#94a3b8;"> Oyun bağlantıları eklendiğinde burada oynayabileceksin. </p> </div> </div> `; } else { iframe.src = gameUrl; iframe.removeAttribute("srcdoc"); } modal.classList.remove("hidden"); document.body.style.overflow = "hidden"; }; // Oyun penceresini kapat window.closeGame = function () { if (!modal || !iframe) { return; } modal.classList.add("hidden"); iframe.src = ""; iframe.removeAttribute("srcdoc"); document.body.style.overflow = ""; }; // Modal dışına tıklayınca kapat if (modal) { modal.addEventListener("click", function (event) { if (event.target === modal) { window.closeGame(); } }); } // ESC ile kapatma document.addEventListener("keydown", function (event) { if (event.key === "Escape") { window.closeGame(); } }); });
